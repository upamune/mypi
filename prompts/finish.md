---
description: Finish the current implementation task end to end
argument-hint: "[goal]"
---

Finish the current task end to end.

Use the repository's existing patterns. Inspect the relevant code before editing. Keep the change scoped to the goal.

Before finalizing:

- Run the most relevant tests or checks available in the repo.
- Check `git diff` and remove unrelated edits.
- Summarize the changed files, verification, and any remaining risk.

$ARGUMENTS
